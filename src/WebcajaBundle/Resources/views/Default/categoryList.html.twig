{% extends 'font.html.twig' %}

{% block body %}
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="{{ path('webcaja_homepage') }}">鸿跃Carsun 订货系统</a>
            </div>
        </div>
    </nav>

    <div class="row" style="padding-bottom: 80px;">
        {% for category in categories %}
            <a href="{{ path('webcaja_productList', { 'id': category.id }) }}" class="fenlei">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 category" data-path="{{ path('webcaja_productList', { 'id': category.id }) }}">
                <div class="col-lg-4 col-md-4 col-sm-4 col-xs-4 categoryfoto">
                    <img src="{{ asset('uploads/fotos/' ~ category.categoryFoto) }}" width=100%>
                </div>
                <div class="col-lg-6 col-md-6 col-sm-6 col-xs-6 categoryname">
                    <h2>{{ category.name }}</h2>
                    <h2>{{ category.nameEs }}</h2>
                </div>
                <div class="col-lg-2 col-md-2 col-sm-2 col-xs-2">
                    <span class="glyphicon glyphicon-menu-right" aria-hidden="true"></span>
                </div>
            </div>
                </a>
        {% endfor %}
    </div>

    <nav class="navbar navbar-inverse navbar-fixed-bottom">
        <div class="container navbuttom">
            <ul class="nav navbar-nav">
                <li><a href="{{ path('webcaja_homepage') }}"><span class="glyphicon glyphicon-home" aria-hidden="true"></span><br>首页</a></li>
                <li class="active"><a href="{{ path('webcaja_categoryList') }}"><span class="glyphicon glyphicon-th-large" aria-hidden="true"></span><br>选购</a></li>
                <li><a href="{{ path('webcaja_info') }}"><span class="glyphicon glyphicon-search" aria-hidden="true"></span><br>用户须知</a></li>
                <li><a href="{{ path('webcaja_carrito') }}"><span class="glyphicon glyphicon-shopping-cart" aria-hidden="true"></span><br>购物车</a></li>
            </ul>
        </div>
    </nav>
{% endblock %}
